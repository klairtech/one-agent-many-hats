node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Local runtime state
.hats-test-home/
.hats/

# Internal — working notes, agent rules and design records. Not published.
CLAUDE.md
docs/
.claude/

# Drafts and internal documents stay local. The published paper is the exception: it is
# what the code implements, so the PDF is tracked deliberately and its sources are not.
*.docx
